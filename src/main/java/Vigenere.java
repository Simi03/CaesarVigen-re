/**
 * @author Simon Lindemann
 * @version 08/04/2021
 */
public class Vigenere {
    public static void main(String[] args) {
      /*  // zweidimensionaler Array für das Verschlüsselungsrechteck
        for (int i = 0; i <= 26; i++) {  // ascii nummer der Verschiebung von a Buchstaben und nummer der Zeile
            for (int r = 0; r < ) {
                int ascii = r + i;
                zweidimensionalerArray[i][r] = ascii;
            }
        }
        // klartext verschlüsseln
        String verschsluesselterText
        for (int f = 0; f <= klartext.length; f++) {    // durch die ganze länge des Texts durch iterieren
            String verschluesselt = zweidimensionalerArray[asciizahlVon(BuchstabeAnStelle(schluesselwort, f))][asciizahlVon(BuchstabeAnStelle(klartext, f))];
            // um den verschlüsselten Buchstaben zu erhalten muss man die asci zahl vom Schlüsselwort als x koordinate und die Ascii Zahl vom klartext als y koordinate
            verschluesselterText = (verschluesselterText + verschluesselt); // jeden Buchstaben zum verschlüsselten Text hinzufügen
        }
        // vom verschlüsselten mit schlüssel entschlüsseln
        String entschluesselterText;
        for (int c = 0; c <= verschluesselterText.length; c++) {
            // Zeile vom Schluesselwort nehmen und dann ascii zahl vom Schluesselwort + ascii Zahl vom verschlüsselten zusamenzählen
            // Wenn es unter 26 ist dies in die y koordinate und wenn es über 26 ist 26 abziehen und in die y koordinate.

        }*/
    }

}
